[project]
name = "expect-py"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = ["oktest"]

[dependency-groups]
dev = [
    "flake8",
    "mypy",
    "pre-commit",
]

[tool.uv]
package = false
default-groups = "all"

[[tool.uv.index]]
name = "pypi"
url = "https://pypi.python.org/simple"
